<div class="container">
  <h1>Yahoo Text Alerts</h1>

  <% if logged_in? %>
    <div>

    </div>
  <% else %>
    <button><a href="/sign_in">Sign In with Twitter</a></button>
  <% end %>

    <% if @error %>
      <p class="error">
        <%= @error %>
      </p>
    <% elsif @message %>
      <p class="message">
        <%= @message %>
      </p>
    <% end %>
  
</div>
